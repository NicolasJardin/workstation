'use client'
import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#D75413'
    },
    secondary: {
      main: '#c5c5c5'
    },
    background: {
      default: '#f2f2f2'
    }
  }
})
