'use client'
import { createTheme } from '@mui/material/styles'

export const theme = createTheme({
  palette: {
    mode: 'light',
    primary: {
      main: '#c5c5c5'
    },
    secondary: {
      main: '#FF5349'
    },
    background: {
      default: '#f2f2f2'
    }
  }
})
